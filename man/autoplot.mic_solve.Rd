% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{autoplot.mic_solve}
\alias{autoplot.mic_solve}
\alias{plot.mic_solve}
\title{autoplot method for mic_solve objects}
\usage{
\method{autoplot}{mic_solve}(
  object,
  type = c("mic", "delta", "ratio", "DoD_delta", "DoD_ratio"),
  x = NULL,
  color_by = NULL,
  ...
)
}
\arguments{
\item{object}{An object returned by \code{\link[=mic_solve]{mic_solve()}}.}

\item{type}{One of \code{"mic"}, \code{"delta"}, or \code{"ratio"}, \code{"DoD_delta"}, or \code{"DoD_ratio"}.}

\item{x}{Variable for x axis plotting}

\item{color_by}{Optional column name used to color and dodge replicate
points. Default: first column in \code{newdata}.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}}.}
}
\value{
A \strong{ggplot} object.
}
\description{
Quick ggplot2 visualisations of the main outputs from
\code{\link[=mic_solve]{mic_solve()}}.  Three panels are supported:
\itemize{
\item \strong{"mic"}   - forest plot of group-wise MIC estimates with
asymmetric CIs.
\item \strong{"delta"} - forest plot of deltaMIC pairwise differences.
\item \strong{"ratio"} - forest plot of MIC ratios (log scale).
}
}
\examples{
if (requireNamespace("ordinal", quietly = TRUE) &&
    requireNamespace("ggplot2", quietly = TRUE)) {
  df <- data.frame(score  = ordered(sample(0:4, 120, TRUE)),
                   conc   = runif(120, 0, 4),
                   strain = factor(sample(c("A","B"), 120, TRUE)))
  fit <- ordinal::clm(score ~ strain * log1p(conc), data = df)
  res <- mic_solve(fit, expand.grid(strain = levels(df$strain)),
                   conc_name = "conc")
  ggplot2::autoplot(res, type = "mic")
}

}
\author{
Carson Stacy
}
