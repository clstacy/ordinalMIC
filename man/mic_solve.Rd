% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mic_solve.R
\name{mic_solve}
\alias{mic_solve}
\title{Estimate MICs and Compare Groups}
\usage{
mic_solve(
  clm_fit,
  newdata = NULL,
  conc_name,
  transform_fun = log1p,
  inv_transform_fun = expm1,
  alpha = 0.05,
  compare_pairs = "all"
)
}
\arguments{
\item{clm_fit}{Fitted object from \code{\link[ordinal:clm]{ordinal::clm()}}.}

\item{newdata}{Data frame with factor combinations to evaluate.}

\item{conc_name}{Character string giving the \strong{raw} concentration column.}

\item{transform_fun}{Transformation used in the model (default \code{log1p}).}

\item{inv_transform_fun}{Inverse transformation (default \code{expm1}).}

\item{alpha}{Confidence‑level significance (default 0.05).}

\item{compare_pairs}{One of \code{"all"} (default) to retain every pairwise
comparison, or \code{"share_any"} to exclude contrasts where the two groups
share no covariate levels in \code{newdata}..}
}
\value{
An object of class \code{"mic_solve"} containing the tables above plus
\code{mic_estimates}.
}
\description{
High‑level wrapper that returns:
\itemize{
\item \code{delta_mic_results} – additive pairwise differences (Δ‑MIC).
\item \code{ratio_mic_results} – multiplicative pairwise ratios.
\item \code{dod_ratio_results} – \strong{difference‑of‑differences on the ratio scale}
(ratio‑of‑ratios, a classic interaction on the log scale).
\item \code{dod_delta_results} – \strong{difference‑of‑differences on the additive scale}
(Δ of Δs).
}
}
\examples{
if (requireNamespace("ordinal", quietly = TRUE)) {
  ## Toy ordinal dataset
  set.seed(1)
  fit <- ordinal::clm(score ~ strain * treatment + log1p(conc), data = yeast_df)
  res <- mic_solve(fit, conc_name = "conc")
  head(res$ratio_mic_results)
}
}
